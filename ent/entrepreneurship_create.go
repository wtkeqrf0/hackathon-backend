// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wtkeqrf0/while.act/ent/entrepreneurship"
)

// EntrepreneurshipCreate is the builder for creating a Entrepreneurship entity.
type EntrepreneurshipCreate struct {
	config
	mutation *EntrepreneurshipMutation
	hooks    []Hook
}

// SetTypes sets the "types" field.
func (ec *EntrepreneurshipCreate) SetTypes(s string) *EntrepreneurshipCreate {
	ec.mutation.SetTypes(s)
	return ec
}

// Mutation returns the EntrepreneurshipMutation object of the builder.
func (ec *EntrepreneurshipCreate) Mutation() *EntrepreneurshipMutation {
	return ec.mutation
}

// Save creates the Entrepreneurship in the database.
func (ec *EntrepreneurshipCreate) Save(ctx context.Context) (*Entrepreneurship, error) {
	return withHooks(ctx, ec.sqlSave, ec.mutation, ec.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ec *EntrepreneurshipCreate) SaveX(ctx context.Context) *Entrepreneurship {
	v, err := ec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ec *EntrepreneurshipCreate) Exec(ctx context.Context) error {
	_, err := ec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ec *EntrepreneurshipCreate) ExecX(ctx context.Context) {
	if err := ec.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ec *EntrepreneurshipCreate) check() error {
	if _, ok := ec.mutation.Types(); !ok {
		return &ValidationError{Name: "types", err: errors.New(`ent: missing required field "Entrepreneurship.types"`)}
	}
	return nil
}

func (ec *EntrepreneurshipCreate) sqlSave(ctx context.Context) (*Entrepreneurship, error) {
	if err := ec.check(); err != nil {
		return nil, err
	}
	_node, _spec := ec.createSpec()
	if err := sqlgraph.CreateNode(ctx, ec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	ec.mutation.id = &_node.ID
	ec.mutation.done = true
	return _node, nil
}

func (ec *EntrepreneurshipCreate) createSpec() (*Entrepreneurship, *sqlgraph.CreateSpec) {
	var (
		_node = &Entrepreneurship{config: ec.config}
		_spec = sqlgraph.NewCreateSpec(entrepreneurship.Table, sqlgraph.NewFieldSpec(entrepreneurship.FieldID, field.TypeInt))
	)
	if value, ok := ec.mutation.Types(); ok {
		_spec.SetField(entrepreneurship.FieldTypes, field.TypeString, value)
		_node.Types = value
	}
	return _node, _spec
}

// EntrepreneurshipCreateBulk is the builder for creating many Entrepreneurship entities in bulk.
type EntrepreneurshipCreateBulk struct {
	config
	builders []*EntrepreneurshipCreate
}

// Save creates the Entrepreneurship entities in the database.
func (ecb *EntrepreneurshipCreateBulk) Save(ctx context.Context) ([]*Entrepreneurship, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ecb.builders))
	nodes := make([]*Entrepreneurship, len(ecb.builders))
	mutators := make([]Mutator, len(ecb.builders))
	for i := range ecb.builders {
		func(i int, root context.Context) {
			builder := ecb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EntrepreneurshipMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ecb *EntrepreneurshipCreateBulk) SaveX(ctx context.Context) []*Entrepreneurship {
	v, err := ecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ecb *EntrepreneurshipCreateBulk) Exec(ctx context.Context) error {
	_, err := ecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ecb *EntrepreneurshipCreateBulk) ExecX(ctx context.Context) {
	if err := ecb.Exec(ctx); err != nil {
		panic(err)
	}
}
