// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/wtkeqrf0/while.act/ent/entrepreneurship"
)

// Entrepreneurship is the model entity for the Entrepreneurship schema.
type Entrepreneurship struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Types holds the value of the "types" field.
	Types        string `json:"types,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Entrepreneurship) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case entrepreneurship.FieldID:
			values[i] = new(sql.NullInt64)
		case entrepreneurship.FieldTypes:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Entrepreneurship fields.
func (e *Entrepreneurship) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case entrepreneurship.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			e.ID = int(value.Int64)
		case entrepreneurship.FieldTypes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field types", values[i])
			} else if value.Valid {
				e.Types = value.String
			}
		default:
			e.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Entrepreneurship.
// This includes values selected through modifiers, order, etc.
func (e *Entrepreneurship) Value(name string) (ent.Value, error) {
	return e.selectValues.Get(name)
}

// Update returns a builder for updating this Entrepreneurship.
// Note that you need to call Entrepreneurship.Unwrap() before calling this method if this Entrepreneurship
// was returned from a transaction, and the transaction was committed or rolled back.
func (e *Entrepreneurship) Update() *EntrepreneurshipUpdateOne {
	return NewEntrepreneurshipClient(e.config).UpdateOne(e)
}

// Unwrap unwraps the Entrepreneurship entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (e *Entrepreneurship) Unwrap() *Entrepreneurship {
	_tx, ok := e.config.driver.(*txDriver)
	if !ok {
		panic("ent: Entrepreneurship is not a transactional entity")
	}
	e.config.driver = _tx.drv
	return e
}

// String implements the fmt.Stringer.
func (e *Entrepreneurship) String() string {
	var builder strings.Builder
	builder.WriteString("Entrepreneurship(")
	builder.WriteString(fmt.Sprintf("id=%v, ", e.ID))
	builder.WriteString("types=")
	builder.WriteString(e.Types)
	builder.WriteByte(')')
	return builder.String()
}

// Entrepreneurships is a parsable slice of Entrepreneurship.
type Entrepreneurships []*Entrepreneurship
